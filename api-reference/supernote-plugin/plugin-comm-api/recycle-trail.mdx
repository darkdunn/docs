---
title: "recycleTrail"
description: "回收 Trail 对象并释放原生缓存。"
---

```ts
static recycleTrail(uuid: string): void;
```

**参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `uuid` | `string` | Trail 的 UUID（见 [`Trail.uuid`](/api-reference/supernote-plugin/types/trail)） |
