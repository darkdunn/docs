---
title: "registerConfigButtonListener"
description: "监听插件配置按钮点击事件。"
---

```ts
registerConfigButtonListener(buttonListener: ConfigButtonListener): ConfigButtonSubscription;
```

**返回**

- `ConfigButtonSubscription`：订阅对象，调用 `remove()` 可注销监听

