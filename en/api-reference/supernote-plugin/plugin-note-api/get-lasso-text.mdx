---
title: "getLassoText"
description: "Get lasso text boxdata."
---

<Note>
You must create a lasso selection before calling this API; otherwise the call fails.

Recommended to call this API from the plugin UI entered via a lasso toolbar button.
</Note>

```ts
static getLassoText(): Promise<APIResponse<TextBox[]>>;
```

**Returns**

- [`APIResponse<TextBox[]>`](/api-reference/supernote-plugin/types/api-response):
`result` is [`TextBox`](/api-reference/supernote-plugin/types/text-box)[] text boxarray

## Example

```ts wrap
import { PluginNoteAPI } from 'sn-plugin-lib';

/**
 * Get lassotext boxlist example.
 */
export async function exampleGetLassoText() {
 const res = await PluginNoteAPI.getLassoText();
 if (!res.success) {
 throw new Error(res.error?.message ?? 'getLassoText call failed');
 }
 return res.result;
}
```
