---
title: "Template"
description: "System note template data."
---

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Template name |
| `vUri` | `string` | Portrait template URI (can be used with React Native built-in `Image`) |
| `hUri` | `string` | Landscape template URI (can be used with React Native built-in `Image`) |

