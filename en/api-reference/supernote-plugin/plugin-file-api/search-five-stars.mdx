---
title: "searchFiveStars"
description: "searchfile in five-starwhere it is located page,returnpage numberlist."
---

```ts wrap
static searchFiveStars(filePath: string): Promise<APIResponse<number[]>>;
```

**Parameters**

| Parameter | Type | Description |
| --- | --- | --- |
| `filePath` | `string` | NOTE/DOCfilepath |

**Returns**

- [`APIResponse<number[]>`](/api-reference/supernote-plugin/types/api-response):
`result` is five-starwhere it is locatedpage numberarray

## Example

```ts wrap
import { PluginFileAPI } from 'sn-plugin-lib';

/**
 * searchfile in five-starwhere it is locatedpage example.
 */
export async function exampleSearchFiveStars() {
 const filePath = '/storage/emulated/0/Note/demo.NOTE';
 const res = await PluginFileAPI.searchFiveStars(filePath);
 if (!res.success) {
 throw new Error(res.error?.message ?? 'searchFiveStars call failed');
 }
 return res.result;
}
```
