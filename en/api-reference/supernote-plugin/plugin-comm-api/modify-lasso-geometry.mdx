---
title: "modifyLassoGeometry"
description: "Modify lassogeometry."
---

<Note>
You must create a lasso selection before calling this API; otherwise the call fails.andMustlassosinglegeometry,otherwisecannotsuccessModify geometry.
</Note>

```ts wrap
static modifyLassoGeometry(geometry: Geometry): Promise<APIResponse<boolean>>;
```

**Parameters**

| Parameter | Type | Description |
| --- | --- | --- |
| `geometry` | [`Geometry`](/en/api-reference/supernote-plugin/types/geometry) | geometryparameter |

**Returns**

- [`APIResponse<boolean>`](/en/api-reference/supernote-plugin/types/api-response):
`result === true` indicatesModify geometrysuccess

## Example

```ts wrap
import { PluginCommAPI, type Geometry } from 'sn-plugin-lib';

/**
 * Modify lassoinonegeometry example.
 */
export async function exampleModifyLassoGeometry() {
 const listRes = await PluginCommAPI.getLassoGeometries();
 if (!listRes.success) {
 throw new Error(listRes.error?.message ?? 'getLassoGeometries call failed');
 }

 const geometries = (listRes.result ?? []) as Geometry[];

 if (geometries.length !== 1) {
 throw new Error('make surecurrentlassoonly includeonegeometry');
 }
 const first = geometries[0];
 if (!first) {
 return false;
 }

 const res = await PluginCommAPI.modifyLassoGeometry(first);
 if (!res.success) {
 throw new Error(res.error?.message ?? 'modifyLassoGeometry call failed');
 }
 return res.result;
}
```
