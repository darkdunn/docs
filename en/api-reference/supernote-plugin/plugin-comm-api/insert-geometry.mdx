---
title: "insertGeometry"
description: "Insert geometry."
---

<Note>
callThis APIwillgeometryInsert to currentopenedfilecurrentpagecurrentlayer.
</Note>

```ts
static insertGeometry(geometry: Geometry): Promise<APIResponse<boolean>>;
```

**Parameters**

| Parameter | Type | Description |
| --- | --- | --- |
| `geometry` | [`Geometry`](/api-reference/supernote-plugin/types/geometry) | geometryparameter |

**Returns**

- [`APIResponse<boolean>`](/api-reference/supernote-plugin/types/api-response):
`result === true` indicatesInsert geometrysuccess

## Example

```ts wrap
import { PluginCommAPI, type Geometry } from 'sn-plugin-lib';

/**
 * Insert onesimplegeometry example.
 */
export async function exampleInsertGeometry() {
 const geometry: Geometry = {
 penColor: 0x9d,
 penType: 10,
 penWidth: 2,
 type: 'GEO_polygon',
 points: [
 { x: 100, y: 100 },
 { x: 200, y: 100 },
 { x: 200, y: 200 },
 { x: 100, y: 200 },
 { x: 100, y: 100 },
 ],
 ellipseCenterPoint: null,
 ellipseMajorAxisRadius: 0,
 ellipseMinorAxisRadius: 0,
 ellipseAngle: 0,
 };

 const res = await PluginCommAPI.insertGeometry(geometry);
 if (!res.success) {
 throw new Error(res.error?.message ?? 'insertGeometry call failed');
 }
 return res.result;
}
```
