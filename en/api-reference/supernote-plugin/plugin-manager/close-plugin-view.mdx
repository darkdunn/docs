---
title: "closePluginView"
description: "closepluginUI."
---

callThis APIwill directlyclosecurrentplugin UIshow.

```ts
closePluginView(): Promise<boolean>;
```

**Returns**

- `Promise<boolean>`:whethersuccessclose

## Example

```ts wrap
import { PluginManager } from 'sn-plugin-lib';

/**
 * closepluginUI example.
 */
export async function exampleClosePluginView() {
 const ok = await PluginManager.closePluginView();
 return ok;
}
```
