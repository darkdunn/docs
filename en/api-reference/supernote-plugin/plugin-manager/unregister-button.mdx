---
title: "unregisterButton"
description: "unregisterpluginbutton."
---

```ts
unregisterButton(id: number): Promise<boolean>;
```

**Parameters**

| Parameter | Type | Description |
| --- | --- | --- |
| `id` | `number` | buttonunique identifier(registerbuttonwhen Set id) |

**Returns**

- `Promise<boolean>`:whethersuccessunregister

## Example

```ts wrap
import { PluginManager } from 'sn-plugin-lib';

/**
 * unregisterbutton example.
 */
export async function exampleUnregisterButton() {
 const ok = await PluginManager.unregisterButton(1);
 return ok;
}
```
